# API interna

As rotas HTTP do AvaliaTech estão em `app/api`, seguindo o padrão de Route Handlers do Next.js App Router.

Este diretório existe para manter a organização solicitada no enunciado do projeto.
